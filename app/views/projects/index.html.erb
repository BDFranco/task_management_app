<!DOCTYPE html>
<html>
<head>
  <title>Task Management</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white min-h-screen">
  <!-- Navigation Bar -->
  <nav class="bg-gray-800 p-4 text-white shadow-md">
    <div class="container mx-auto flex justify-between items-center">
      <a href="/" class="text-xl font-bold">Task Manager</a>
      <div class="flex space-x-4">
        <%= link_to "Profile", profile_path, class: "hover:text-gray-300" %>
        <% if user_signed_in? %>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "hover:text-gray-300" %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "hover:text-gray-300" %>
        <% end %>
      </div>
    </div>
  </nav>

  <div class="flex items-center justify-center p-6">
    <div class="bg-white shadow-lg rounded-lg p-6 w-full max-w-4xl">
      <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Tasks</h1>

      <% if current_user.respond_to?(:role) && current_user.role == "admin" %>
        <div class="flex justify-end mb-4">
          <%= link_to "New Project", new_project_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
        </div>
      <% end %>

      <div class="overflow-hidden rounded-lg border border-gray-200 shadow-md">
        <table class="min-w-full bg-white">
          <thead>
            <tr class="w-full bg-gray-100 border-b">
              <th class="py-3 px-6 text-left text-gray-700">Name</th>
              <th class="py-3 px-6 text-left text-gray-700">Description</th>
              <th class="py-3 px-6 text-left text-gray-700">Created By</th>
              <th class="py-3 px-6 text-center text-gray-700">Actions</th>
            </tr>
          </thead>
          <tbody>
            <% @projects.each do |project| %>
              <tr class="border-b hover:bg-gray-50">
                <td class="py-3 px-6"><%= project.name %></td>
                <td class="py-3 px-6"><%= project.description %></td>
                <td class="py-3 px-6"><%= project.created_by %></td>
                <td class="py-3 px-6 text-center">
                  <%= link_to "View", project_path(project), class: "bg-green-500 hover:bg-green-700 text-white font-bold py-1 px-3 rounded" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>
</html>