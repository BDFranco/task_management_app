<!DOCTYPE html>
<html>
<head>
  <title>Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="bg-white shadow-lg rounded-lg p-8 max-w-lg w-full">
    <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Profile</h1>
    
    <div class="flex items-center space-x-4 mb-6">
      <div class="w-16 h-16 bg-gray-300 rounded-full flex items-center justify-center text-gray-700 text-2xl font-bold">
        <%= current_user.email[0].upcase %>
      </div>
      <div>
        <p class="text-xl font-semibold text-gray-800"><%= current_user.email %></p>
        <p class="text-gray-600"><%= current_user.role.capitalize if current_user.respond_to?(:role) %></p>
      </div>
    </div>
    
    <div class="border-t pt-4">
      <p class="text-gray-700 mb-2"><strong>Name:</strong> <%= current_user.name if current_user.respond_to?(:name) %></p>
      <p class="text-gray-700 mb-2"><strong>Email:</strong> <%= current_user.email %></p>
      <p class="text-gray-700"><strong>Joined:</strong> <%= current_user.created_at.strftime("%B %d, %Y") %></p>
    </div>
    
    <div class="mt-6 flex justify-center">
    <%= link_to "Edit Profile", edit_profile_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
  </div>
</body>
</html>